version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - node -v
        - export NODE_OPTIONS="--max-old-space-size=6144"
        - npm cache clean --force
        - npm install --legacy-peer-deps --no-audit
    build:
      commands:
        - export NODE_OPTIONS="--max-old-space-size=6144"
        - |
          cat > .env.production <<EOF
          NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL:-https://www.nesting-place.com}
          NEXT_PUBLIC_AWS_REGION=${NEXT_PUBLIC_AWS_REGION:-us-east-1}
          NEXT_PUBLIC_USER_POOL_ID=${NEXT_PUBLIC_USER_POOL_ID}
          NEXT_PUBLIC_USER_POOL_CLIENT_ID=${NEXT_PUBLIC_USER_POOL_CLIENT_ID}
          NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL}
          NEXT_PUBLIC_COGNITO_DOMAIN=${NEXT_PUBLIC_COGNITO_DOMAIN}
          NEXT_PUBLIC_SOCIAL_AUTH_ENABLED=${NEXT_PUBLIC_SOCIAL_AUTH_ENABLED}
          NEXT_PUBLIC_AUTH_GOOGLE_ENABLED=${NEXT_PUBLIC_AUTH_GOOGLE_ENABLED}
          NEXT_PUBLIC_AUTH_FACEBOOK_ENABLED=${NEXT_PUBLIC_AUTH_FACEBOOK_ENABLED}
          NEXT_PUBLIC_AUTH_APPLE_ENABLED=${NEXT_PUBLIC_AUTH_APPLE_ENABLED}
          TASKS_S3_BUCKET=${TASKS_S3_BUCKET:-nurture-collective-tasks}
          INTAKE_S3_BUCKET=${INTAKE_S3_BUCKET:-${TASKS_S3_BUCKET:-nurture-collective-tasks}}
          COMMUNITY_MEDIA_S3_BUCKET=${COMMUNITY_MEDIA_S3_BUCKET:-${TASKS_S3_BUCKET:-nurture-collective-tasks}}
          COMMUNITY_API_URL=${COMMUNITY_API_URL}
          COMMUNITY_DEMO_FALLBACK=${COMMUNITY_DEMO_FALLBACK}
          COMMUNITY_ENV_SCOPE=${COMMUNITY_ENV_SCOPE:-production}
          NEXT_PUBLIC_COMMUNITY_ENV_SCOPE=${COMMUNITY_ENV_SCOPE:-production}
          AWS_REGION=${NEXT_PUBLIC_AWS_REGION:-us-east-1}
          TASKS_S3_KEY=${TASKS_S3_KEY}
          MANAGEMENT_COGNITO_GROUP=${MANAGEMENT_COGNITO_GROUP:-admin}
          NEXT_PUBLIC_MANAGEMENT_COGNITO_GROUP=${NEXT_PUBLIC_MANAGEMENT_COGNITO_GROUP:-admin}
          NEXT_PUBLIC_INTAKE_PUBLIC=${NEXT_PUBLIC_INTAKE_PUBLIC:-true}
          INTAKE_PUBLIC=${INTAKE_PUBLIC:-true}
          NEXT_PUBLIC_GOOGLE_REVIEWS_VISIBILITY=${NEXT_PUBLIC_GOOGLE_REVIEWS_VISIBILITY:-public}
          GOOGLE_REVIEWS_MODE=${GOOGLE_REVIEWS_MODE:-placeholder}
          GOOGLE_PLACE_ID=${GOOGLE_PLACE_ID}
          GOOGLE_PLACES_API_KEY=${GOOGLE_PLACES_API_KEY}
          NEXT_PUBLIC_GOOGLE_REVIEWS_URL=${NEXT_PUBLIC_GOOGLE_REVIEWS_URL}
          NEXT_PUBLIC_BOOKING_PROVIDER=${NEXT_PUBLIC_BOOKING_PROVIDER:-google}
          NEXT_PUBLIC_CALENDLY_URL=${NEXT_PUBLIC_CALENDLY_URL}
          NEXT_PUBLIC_GOOGLE_BOOKING_URL=${NEXT_PUBLIC_GOOGLE_BOOKING_URL:-https://calendar.google.com/calendar/appointments/schedules/AcZssZ3Q0wMuGu20kSp9Vqk7iNhSc8QBEeT4nZGIBLSvCGtlXiP19t27By7jJa0y77koQzPRYRTdhAuL?gv=true}
          NEXT_PUBLIC_CONTACT_EMAIL=${NEXT_PUBLIC_CONTACT_EMAIL:-info@nesting-place.com}
          NEXT_PUBLIC_BLOCK_SEARCH_INDEXING=${NEXT_PUBLIC_BLOCK_SEARCH_INDEXING:-false}
          NEXT_PUBLIC_WHATSAPP_NUMBER=${NEXT_PUBLIC_WHATSAPP_NUMBER}
          SERVER_AWS_ACCESS_KEY_ID=${SERVER_AWS_ACCESS_KEY_ID}
          SERVER_AWS_SECRET_ACCESS_KEY=${SERVER_AWS_SECRET_ACCESS_KEY}
          OPENAI_API_KEY=${OPENAI_API_KEY}
          OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
          NURTURE_LEADS_BUCKET=${NURTURE_LEADS_BUCKET}
          NURTURE_EVENTS_BUCKET=${NURTURE_EVENTS_BUCKET}
          SLACK_ENABLED=${SLACK_ENABLED}
          SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
          SLACK_WEBHOOK_NEW_LEADS=${SLACK_WEBHOOK_NEW_LEADS}
          SLACK_WEBHOOK_SCHEDULED_CALLS=${SLACK_WEBHOOK_SCHEDULED_CALLS}
          SLACK_WEBHOOK_OPERATIONS=${SLACK_WEBHOOK_OPERATIONS}
          SLACK_WEBHOOK_LOST_LEADS=${SLACK_WEBHOOK_LOST_LEADS}
          SLACK_CHANNEL_NEW_LEADS=${SLACK_CHANNEL_NEW_LEADS}
          SLACK_CHANNEL_SCHEDULED_CALLS=${SLACK_CHANNEL_SCHEDULED_CALLS}
          SLACK_CHANNEL_OPERATIONS=${SLACK_CHANNEL_OPERATIONS}
          SLACK_CHANNEL_LOST_LEADS=${SLACK_CHANNEL_LOST_LEADS}
          CALENDLY_WEBHOOK_SIGNING_KEY=${CALENDLY_WEBHOOK_SIGNING_KEY}
          N8N_INQUIRY_WEBHOOK_URL=${N8N_INQUIRY_WEBHOOK_URL}
          N8N_WEBHOOK_URL=${N8N_WEBHOOK_URL}
          N8N_WEBHOOK_SECRET=${N8N_WEBHOOK_SECRET}
          N8N_TEAM_NOTIFICATION_EMAIL=${N8N_TEAM_NOTIFICATION_EMAIL:-info@nesting-place.com}
          INTAKE_TIMEOUT=${INTAKE_TIMEOUT:-15}
          MAX_RETRIES=${MAX_RETRIES:-3}
          LEADS_BUCKET=${LEADS_BUCKET:-${NURTURE_LEADS_BUCKET}}
          N8N_TASK_SYNC_WEBHOOK_URL=${N8N_TASK_SYNC_WEBHOOK_URL}
          GOOGLE_TASKS_ENABLED=${GOOGLE_TASKS_ENABLED}
          GOOGLE_TASKS_SYNC_MODE=${GOOGLE_TASKS_SYNC_MODE}
          GOOGLE_TASKS_AUTH_MODE=${GOOGLE_TASKS_AUTH_MODE}
          GOOGLE_TASKS_DELEGATED_USER=${GOOGLE_TASKS_DELEGATED_USER:-admin@nesting-place.com}
          GOOGLE_TASKS_IMPERSONATE_SERVICE_ACCOUNT=${GOOGLE_TASKS_IMPERSONATE_SERVICE_ACCOUNT}
          GOOGLE_TASKS_LIST_ID=${GOOGLE_TASKS_LIST_ID}
          GOOGLE_TASKS_LIST_TITLE=${GOOGLE_TASKS_LIST_TITLE}
          GOOGLE_TASKS_ADC_JSON=${GOOGLE_TASKS_ADC_JSON}
          GOOGLE_TASKS_OAUTH_CLIENT_ID=${GOOGLE_TASKS_OAUTH_CLIENT_ID}
          GOOGLE_TASKS_OAUTH_CLIENT_SECRET=${GOOGLE_TASKS_OAUTH_CLIENT_SECRET}
          GOOGLE_TASKS_OAUTH_REDIRECT_URI=${GOOGLE_TASKS_OAUTH_REDIRECT_URI}
          GOOGLE_TASKS_OAUTH_REFRESH_TOKEN=${GOOGLE_TASKS_OAUTH_REFRESH_TOKEN}
          GOOGLE_SERVICE_ACCOUNT_EMAIL=${GOOGLE_SERVICE_ACCOUNT_EMAIL}
          GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=${GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY}
          GOOGLE_SERVICE_ACCOUNT_JSON=${GOOGLE_SERVICE_ACCOUNT_JSON}
          GOOGLE_CALENDAR_ENABLED=${GOOGLE_CALENDAR_ENABLED:-true}
          GOOGLE_CALENDAR_AUTH_MODE=${GOOGLE_CALENDAR_AUTH_MODE:-delegated}
          GOOGLE_WORKLOAD_IDENTITY_PROJECT_NUMBER=${GOOGLE_WORKLOAD_IDENTITY_PROJECT_NUMBER}
          GOOGLE_WORKLOAD_IDENTITY_POOL_ID=${GOOGLE_WORKLOAD_IDENTITY_POOL_ID}
          GOOGLE_WORKLOAD_IDENTITY_PROVIDER_ID=${GOOGLE_WORKLOAD_IDENTITY_PROVIDER_ID}
          GOOGLE_WORKLOAD_IDENTITY_SERVICE_ACCOUNT=${GOOGLE_WORKLOAD_IDENTITY_SERVICE_ACCOUNT:-${GOOGLE_TASKS_IMPERSONATE_SERVICE_ACCOUNT}}
          GOOGLE_CALENDAR_ID=${GOOGLE_CALENDAR_ID:-c_2d5a066a46512e1ec02b55c8c92e83e00a9a8e77655de2e712a347fbb969552c@group.calendar.google.com}
          CLASS_EVENTS_GOOGLE_CALENDAR_ID=${CLASS_EVENTS_GOOGLE_CALENDAR_ID:-c_6c4c40e2772622371ffcd13d9f51dd52474ad5f05740f10faf1d4468a272f415@group.calendar.google.com}
          CLASS_EVENTS_CALENDAR_SYNC_ENABLED=${CLASS_EVENTS_CALENDAR_SYNC_ENABLED:-true}
          GOOGLE_CALENDAR_DELEGATED_USER=${GOOGLE_CALENDAR_DELEGATED_USER:-admin@nesting-place.com}
          GOOGLE_CALENDAR_ADC_JSON=${GOOGLE_CALENDAR_ADC_JSON:-${GOOGLE_TASKS_ADC_JSON}}
          GOOGLE_TASKS_IMPERSONATE_SERVICE_ACCOUNT=${GOOGLE_TASKS_IMPERSONATE_SERVICE_ACCOUNT:-nurture-tasks-sync@boxwood-magnet-498623-n4.iam.gserviceaccount.com}
          GOOGLE_BOOKING_DURATION_MINUTES=${GOOGLE_BOOKING_DURATION_MINUTES:-30}
          GOOGLE_BOOKING_BUFFER_MINUTES=${GOOGLE_BOOKING_BUFFER_MINUTES:-15}
          GOOGLE_BOOKING_TIMEZONE=${GOOGLE_BOOKING_TIMEZONE:-America/New_York}
          GOOGLE_BOOKING_WORK_HOURS_START=${GOOGLE_BOOKING_WORK_HOURS_START:-09:00}
          GOOGLE_BOOKING_WORK_HOURS_END=${GOOGLE_BOOKING_WORK_HOURS_END:-17:00}
          GOOGLE_BOOKING_WORK_DAYS=${GOOGLE_BOOKING_WORK_DAYS:-1,2,3,4,5}
          GOOGLE_BOOKING_HORIZON_DAYS=${GOOGLE_BOOKING_HORIZON_DAYS:-14}
          GOOGLE_BOOKING_MIN_LEAD_HOURS=${GOOGLE_BOOKING_MIN_LEAD_HOURS:-2}
          N8N_GOOGLE_BOOKINGS_WEBHOOK_URL=${N8N_GOOGLE_BOOKINGS_WEBHOOK_URL}
          GOOGLE_BOOKINGS_WEBHOOK_SECRET=${GOOGLE_BOOKINGS_WEBHOOK_SECRET}
          N8N_CALENDLY_WEBHOOK_URL=${N8N_CALENDLY_WEBHOOK_URL}
          N8N_BILLING_WEBHOOK_URL=${N8N_BILLING_WEBHOOK_URL}
          N8N_BILLING_WEBHOOK_SECRET=${N8N_BILLING_WEBHOOK_SECRET}
          BILLING_SYNC_MODE=${BILLING_SYNC_MODE:-n8n}
          BILLING_PAYMENT_PROVIDER=${BILLING_PAYMENT_PROVIDER:-stub}
          BILLING_S3_BUCKET=${BILLING_S3_BUCKET:-${NURTURE_CLIENTS_BUCKET}}
          QBO_ENVIRONMENT=${QBO_ENVIRONMENT:-sandbox}
          QBO_CLIENT_ID=${QBO_CLIENT_ID}
          QBO_CLIENT_SECRET=${QBO_CLIENT_SECRET}
          QBO_REALM_ID=${QBO_REALM_ID}
          QBO_REFRESH_TOKEN=${QBO_REFRESH_TOKEN}
          QBO_REDIRECT_URI=${QBO_REDIRECT_URI:-${NEXT_PUBLIC_APP_URL}/api/integrations/quickbooks/oauth/callback}
          QBO_WEBHOOK_VERIFIER=${QBO_WEBHOOK_VERIFIER}
          QBO_DEFAULT_ITEM_ID=${QBO_DEFAULT_ITEM_ID}
          GIFT_CARD_PAYMENT_PROVIDER=${GIFT_CARD_PAYMENT_PROVIDER:-stub}
          GIFT_CARD_ORDER_WEBHOOK_URL=${GIFT_CARD_ORDER_WEBHOOK_URL}
          GIFT_CARD_ORDER_WEBHOOK_SECRET=${GIFT_CARD_ORDER_WEBHOOK_SECRET}
          GIFT_CARD_EMAIL_ENABLED=${GIFT_CARD_EMAIL_ENABLED}
          GIFT_CARD_EMAIL_PROVIDER=${GIFT_CARD_EMAIL_PROVIDER:-auto}
          GIFT_CARD_EMAIL_FROM=${GIFT_CARD_EMAIL_FROM}
          GIFT_CARD_EMAIL_FROM_NAME=${GIFT_CARD_EMAIL_FROM_NAME}
          GIFT_CARD_EMAIL_REPLY_TO=${GIFT_CARD_EMAIL_REPLY_TO}
          GIFT_CARD_FULFILLMENT_EMAIL=${GIFT_CARD_FULFILLMENT_EMAIL}
          RESEND_API_KEY=${RESEND_API_KEY}
          NEXT_PUBLIC_GIFT_CARD_PAYMENTS_ENABLED=${NEXT_PUBLIC_GIFT_CARD_PAYMENTS_ENABLED:-false}
          NEXT_PUBLIC_GIFT_CARD_PAYMENT_PROVIDER=${NEXT_PUBLIC_GIFT_CARD_PAYMENT_PROVIDER:-stub}
          STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
          STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
          TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
          TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
          TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER}
          NEXT_PUBLIC_TWILIO_PHONE_NUMBER=${NEXT_PUBLIC_TWILIO_PHONE_NUMBER:-${TWILIO_PHONE_NUMBER}}
          TWILIO_SMS_WEBHOOK_URL=${TWILIO_SMS_WEBHOOK_URL}
          EOF
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
