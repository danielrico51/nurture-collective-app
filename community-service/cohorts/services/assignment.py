"""Cohort assignment services — hard-coded logic, implement Sprint 3."""

# TODO: assign_pregnancy_cohort(user_id, profile) -> list[CohortAssignment]
# TODO: assign_postpartum_cohort(user_id, profile) -> list[CohortAssignment]
# TODO: assign_newborn_cohort(user_id, profile) -> list[CohortAssignment]
# TODO: assign_all_cohorts(user_id, profile, force_refresh=False) -> list[CohortAssignment]
